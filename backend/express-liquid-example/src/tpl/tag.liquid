<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    {% upper "hello" %}
    {{ 1 | add: 2, 3 }}
    {% rand foo bar coo %}
    {% wrap %}
    {{ "hello world!" | capitalize }}
    {% endwrap %}
    {%- assign arr = "1,2,3" | split: "," -%}
    {%- for i in (1..3) -%}
    {%- assign arr = arr | concat: arr -%}
    {%- endfor -%}
    {% if arr %}
    {{ arr | join: "," -}}
    {% endif %}
</body>

</html>