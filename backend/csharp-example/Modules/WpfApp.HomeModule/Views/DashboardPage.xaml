<UserControl x:Class="WpfApp.HomeModule.Views.DashboardPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp.HomeModule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="30" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Border Background="White" CornerRadius="5"  Padding="5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Color="Gray" Opacity="0.1" />
            </Border.Effect>
        </Border>
        <Border Background="White" CornerRadius="5" Grid.Row="1" Padding="5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Color="Gray" Opacity="0.1" />
            </Border.Effect>
        </Border>
        <Border Background="White" CornerRadius="5" Grid.Row="2" Padding="5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="0" Color="Gray" Opacity="0.1" />
            </Border.Effect>
        </Border>

        <TextBlock Text="Dashboard" />
        <ListBox ItemsSource="{Binding TabItems}"></ListBox>
    </Grid>
</UserControl>
